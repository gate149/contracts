{
  "name": "contracts",
  "packageManager": "npm@latest",
  "scripts": {
    "gen": "openapi --input core/v1/openapi.yaml --output ./core/v1 --client fetch --name core --useOptions"
  },
  "devDependencies": {
    "openapi-typescript-codegen": "^0.29.0",
    "typescript": "^5.9.2"
  }
}
